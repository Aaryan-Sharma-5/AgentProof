# DEPRECATED — Fly.io configuration

**These files are historical and are not the current deployment path.**

AgentProof deploys to **Vercel (frontend) + Render (backend services)**. See
[../README.md](../README.md) and the repository-root `render.yaml`.

These Fly.io configs were written during Phase 6C, before the platform decision was finalised.
They were never used for a live deployment. They are kept only for reference and should not be
followed. Nothing in the project reads them.
